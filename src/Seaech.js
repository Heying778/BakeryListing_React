const Search = () => {
    return (
        <div>
            <button key = "submit">Serch</button>
            <form key = "get">
                <input key = "text" placeholder="Type in here" />
            </form>
        </div>
    );
}
export default Search;